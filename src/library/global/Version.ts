export const VersionController = new class {
    constructor() {
    }
    CheckVersion() {
        let appVersion = document.querySelector("meta[name='app-version']")?.getAttribute("content") ?? "";
        // if (appVersion != )
    }
}